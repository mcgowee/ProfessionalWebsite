export declare class SvelteEventManager {
    elementMap: Map<Element | Window, {
        [k: string]: Map<EventListener, Function>;
    }>;
    on(element: Element | Window, event: string, handler: EventListener): void;
    off(element: Element, event: string, handler: EventListener): void;
}
